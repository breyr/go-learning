package integers

// x and y are both ints
// having more than one argument of the same type you can do this
// don't need to use a named return since its inferred from the function name
// comments above functions will redner in the godoc

// Add takes two integers and retuns the sum of them.
func Add(x, y int) int {
	return x + y
}
